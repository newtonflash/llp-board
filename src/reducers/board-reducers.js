const initialState = {
    title: "",
    taskList : []
}
const BoardReducers = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default BoardReducers;